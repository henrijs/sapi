{#
/**
 * @file sapi_storage.html.twig
 * Default theme implementation to present Statistic data.
 *
 * This template is used when viewing Statistic pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_sapi_storage()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('sapi_storage') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
